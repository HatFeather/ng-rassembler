import { RassemblyDirective } from './rassembly.directive';

describe('RassemblyDirective', () => {
  it('should create an instance', () => {
    const directive = new RassemblyDirective();
    expect(directive).toBeTruthy();
  });
});
